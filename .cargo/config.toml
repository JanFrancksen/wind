# Run macOS development builds from the persistent CEF app bundle. The runner
# builds the bundle once, then only replaces the application and helper binaries.
[target.'cfg(target_os = "macos")']
runner = ["scripts/run-bundled-wind"]
